import React from "react"
import "../sass/NotFound.sass"

export default ({ children }) => (
    <div className="NotFound">
        <div className="inner">
            <h1>Not Found</h1>
        </div>
    </div>
)